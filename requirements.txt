# ScholarGuard pipeline (CPU-only). For the web UI backend see server/requirements.txt.

# Core CV + numerics
opencv-python==4.13.0.92
numpy==2.5.0
scikit-learn==1.7.1
scipy==1.16.1
matplotlib==3.10.6
pillow==12.0.0

# Cross-figure duplicate detection
imagehash==4.3.2
faiss-cpu==1.14.3
tqdm==4.67.1

# AI-generation forensics (frequency + wavelet noise residual)
PyWavelets==1.9.0
# Install the CPU-only torch wheels:
#   pip install torch==2.9.0 torchvision==0.24.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.9.0
torchvision==0.24.0

# Pipeline config
PyYAML==6.0.3

# Data acquisition (PubMed Central Open Access + Retraction Watch)
requests==2.32.4
pandas==2.3.1

# NLP claim-consistency (PDF parsing + Claude API; key read from env, never hardcoded)
pymupdf==1.28.0
anthropic==0.116.0
python-dotenv==1.1.1
sentence-transformers==5.1.2

# Tests
pytest==8.4.2
