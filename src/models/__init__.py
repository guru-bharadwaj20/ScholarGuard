"""Learned models for ScholarGuard.

Stage 4 ships an artifact classifier *definition* and inference-only
loader; its weights are trained separately on GPU (Colab/Kaggle) — never
in this codebase. See :mod:`src.models.artifact_classifier`.
"""
